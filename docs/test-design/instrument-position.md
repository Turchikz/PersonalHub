## Сценарии проверки СИ
| ID     | Запрос и исходное состояние          | Данные запроса                     | Ожидаемый результат                         |
| ------ | ------------------------------------ | ---------------------------------- | ------------------------------------------- |
| INS-01 | `POST`                               | `WORK`, позиция `PDT-2001`         | `201`, позиция сохранена                    |
| INS-02 | `POST`                               | `WORK`, позиция отсутствует        | `400`, ошибка `position`, запись не создана |
| INS-03 | `POST`                               | `WORK`, позиция `PDT-2001`         | `201`, сохранено `PDT-2001`                 |
| INS-04 | `POST`                               | `SPARE`, позиция `PDT-2001`        | `201`, сохранена позиция `null`             |
| INS-05 | `POST`                               | `REPAIR`, позиция `PDT-2001`       | `201`, сохранена позиция `null`             |
| INS-06 | `PATCH`, прибор `REPAIR` без позиции | Статус `WORK`, позиция не передана | `400`, прибор остаётся `REPAIR` без позиции |
| INS-07 | `PATCH`, прибор `REPAIR` без позиции | Статус `WORK`, позиция `PDT-2008`  | `200`, сохранены `WORK` и `PDT-2008`        |
| INS-08 | `POST`                               | `VERIFICATION`, позиция `PDT-2001` | `201`, сохранена позиция `null`             |
___
## Сценарии проверки поверок

| ID | Запрос и исходное состояние | Даты в запросе | Ожидаемый результат |
| --- | --- | --- | --- |
| VER-01 | `POST`, прибор существует | `verification_date` — сегодня; `valid_until` — через 365 дней; `next_verification_date` отсутствует | `201`, поверка сохранена, `next_verification_date = null` |
| VER-02 | `POST`, прибор существует | `verification_date` — завтра; `valid_until` — через 365 дней | `400`, ошибка `verification_date`, поверка не создана |
| VER-03 | `POST`, прибор существует | `verification_date` — сегодня; `valid_until` — сегодня | `400`, ошибка `valid_until`, поверка не создана |
| VER-04 | `POST`, прибор существует | `verification_date` — сегодня; `valid_until` — через 365 дней; `next_verification_date` — сегодня | `400`, ошибка `next_verification_date`, поверка не создана |
| VER-05 | `PATCH`, сохранённая поверка действует ещё 365 дней | Изменить только `valid_until` на дату поверки | `400`, ошибка `valid_until`, сохранённая дата окончания не изменилась |

Сценарии составлены по бизнес-правилам моделей Instrument и Verification.
Автоматизированные проверки находятся в `metrology/tests/`.
Таблица описывает ожидаемое поведение API; наличие сценария в таблице само по себе не означает, что для него уже написан отдельный тест.